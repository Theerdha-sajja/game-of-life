pipeline{
		agent any
	stages{
		       stage('three'){
			               
					steps{
	                                         sh "mvn --version"
						 sh "java --version"
						sh "git --version"
						sh "ls"
					      }
				            }

			   }
	    }
